type Query {
  version: String
  # See each individual schema for specific queries
}

type Mutation {
  random(byteLength: Int!): String!
  # See each individual schema for specific queries
}

schema {
  query: Query
  mutation: Mutation
}
